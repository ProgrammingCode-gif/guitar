const ROOT_PRODUCT = document.getElementById('product');
const ROOT_SHOPPING = document.getElementById('shopping');
const ROOT_HEADER = document.getElementById('header');
const ROOT_LOADER = document.getElementById('loader');